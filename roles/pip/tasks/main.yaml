---

- name: pip - main
  include_role:
    public: yes
    name: common
    tasks_from: main
  vars:
    given: "{{ pip }}"


- name: update
  command: "{{ item }} -m pip --no-cache-dir install --upgrade pip"
  with_items:
    - "python2"
    - "python3"
  become: yes