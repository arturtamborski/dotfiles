---

- name: update system
  become: yes
  apt:
    upgrade: dist
    update_cache: yes


- name: install dependencies
  become: yes
  package:
    name: "{{ item }}"
    state: present
  with_items: "{{ apt.dependencies }}"
